name: "MediaCraft AI — Multi-Worker"

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch:

jobs:
  worker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Run dealwork.ai Worker
        continue-on-error: true
        env:
          DEALWORK_API_KEY: ${{ secrets.DEALWORK_API_KEY }}
        run: node dealwork_worker.js

      - name: Run Dev.to Content Publisher
        continue-on-error: true
        env:
          DEVTO_API_KEY: ${{ secrets.DEVTO_API_KEY }}
        run: node devto_worker.js

      - name: Run Hashnode Content Publisher
        continue-on-error: true
        env:
          HASHNODE_API_KEY: ${{ secrets.HASHNODE_API_KEY }}
        run: node hashnode_worker.js

      - name: Run TaskMarket Worker
        continue-on-error: true
        run: node taskmarket_worker.js

      - name: Run Superteam Earn Worker
        continue-on-error: true
        env:
          SUPERTEAM_API_KEY: ${{ secrets.SUPERTEAM_API_KEY }}
        run: node superteam_worker.js

      - name: Run Toku Agency Worker
        continue-on-error: true
        env:
          TOKU_API_KEY: ${{ secrets.TOKU_API_KEY }}
        run: node toku_worker.js

      - name: Run DeFi Simulator (daily)
        continue-on-error: true
        run: node defi_simulator.js run

      - name: Commit learning data
        continue-on-error: true
        run: |
          git config user.name "MediaCraft AI"
          git config user.email "bot@mediacraft.ai"
          git add data/ logs/ --all
          git diff --cached --quiet || git commit -m "[auto] learning update $(date +%Y-%m-%d)"
          git push || true

  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render daemon (prevent sleep)
        run: |
          curl -s -o /dev/null -w "%{http_code}" https://mediacraft-x402-api.onrender.com/daemon/health || true
